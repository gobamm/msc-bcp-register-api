{
  "version": "0.2.0",
  "configurations": [
    
    {
      "name": "BCP SSO TOKEN API",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "cwd": "${workspaceFolder}/src/app",
      "args": ["main:app", "--port", "3500", "--reload"]
    }
  ]
}
